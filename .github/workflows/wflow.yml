name: setup/tests/lint
on: push
jobs:
  build: 
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - name: run setup
        run: make setup
      - name: run tests
        run: make test
      - name: run linter
        run: make lint
